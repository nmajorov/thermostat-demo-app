apiVersion: v1
kind: Service
metadata:
  name: {{ include "thermostat-chart.fullname" . }}
  labels:
    {{- include "thermostat-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "thermostat-chart.selectorLabels" . | nindent 4 }}
